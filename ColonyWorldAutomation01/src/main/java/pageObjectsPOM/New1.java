package pageObjectsPOM;

public class New1 {
/*
    // Locate the table element representing the calendar
    WebElement calendarTable = driver.findElement(By.id("calendar"));

    // Locate the specific date element within the table (assuming the date is in a cell with a specific class)
    List<WebElement> dateElements = calendarTable.findElements(By.className("date-cell"));

    // Iterate through the date elements and click on the desired date (e.g., "25")
    String desiredDate = "25"; // Change this to the desired date
    for (WebElement dateElement : dateElements) {
        String dateText = dateElement.getText();
        if (dateText.equals(desiredDate)) {
            dateElement.click();
            break; // Break the loop once the desired date is found and clicked
        }
    }*/
}
